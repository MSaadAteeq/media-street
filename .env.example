# Frontend environment variables
# Copy to .env.local for local development

# API Base URL - REQUIRED for production (Vercel)
# Local: defaults to http://localhost:3000/api/v1/
# Production: Set to your Render backend URL, e.g. https://ms-backend-offerave-xxx.onrender.com/api/v1/
# Without this, "Request a FREE Tablet" and other API calls will fail with "No response from server"
VITE_API_BASE_URL=http://localhost:3000/api/v1/
